Generated personal study data belongs here.

`catalog.json` is intentionally ignored by git. The deployed app can load it when you build a private copy, and the public GitHub Pages version can still import the same JSON file through the browser.
